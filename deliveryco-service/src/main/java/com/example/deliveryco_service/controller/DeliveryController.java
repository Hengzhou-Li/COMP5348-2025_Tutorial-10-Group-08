package com.example.deliveryco_service.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("/api/deliveries")
public class DeliveryController {

    // TODO: Add delivery management endpoints
    // - GET /api/deliveries/{deliveryId} - Get delivery status
    // - POST /api/deliveries/{deliveryId}/track - Update tracking info
    // - GET /api/deliveries/{deliveryId}/events - Get delivery events

}

